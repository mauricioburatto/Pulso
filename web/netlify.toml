# Config do Netlify para o frontend do Pulso. A Vercel detecta um projeto
# Vite automaticamente e não precisa de arquivo equivalente.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
